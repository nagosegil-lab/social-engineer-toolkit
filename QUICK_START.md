# ⚡ התחלה מהירה - בוט מסחר נייד

## 🚀 התקנה ב-5 דקות

### 1️⃣ הורדה והתקנה
```bash
# שכפל הפרויקט
git clone <repository-url>
cd mobile-trading-bot

# הרץ התקנה אוטומטית
python3 setup_bot.py

# התקן חבילות
pip3 install -r requirements_trading.txt
```

### 2️⃣ הגדרה בסיסית
```bash
# העתק קובץ הגדרות
cp .env.example .env

# ערוך הגדרות MT5 (חובה!)
nano .env
```

**הגדרות מינימליות ב-.env:**
```bash
MT5_SERVER=demo-server
MT5_LOGIN=your_demo_account
MT5_PASSWORD=your_password
SECRET_KEY=change-this-secret-key
```

### 3️⃣ הפעלה
```bash
# הפעל הבוט
python3 main.py

# או עם הסקריפט
./start_bot.sh  # Linux/Mac
start_bot.bat   # Windows
```

### 4️⃣ גישה מהטלפון
1. פתח דפדפן בטלפון
2. גש ל-`http://YOUR_COMPUTER_IP:8080`
3. התחבר עם סיסמה: `trading123`

## 📱 שימוש ראשוני

### דשבורד ראשי
- **סטטוס הבוט** - רץ/עצור
- **יתרת חשבון** - כסף זמין
- **רווח/הפסד יומי** - ביצועים היום
- **פוזיציות פתוחות** - עסקאות פעילות

### כפתורי בקרה
- **▶️ Start** - הפעל מסחר אוטומטי
- **⏹️ Stop** - עצור מסחר
- **🔄 Refresh** - עדכן נתונים
- **⚙️ Settings** - הגדרות סיכון

### הגדרות בסיסיות
- **סיכון לעסקה**: 2% (מומלץ להתחיל)
- **הפסד יומי מקסימלי**: 5%
- **מספר פוזיציות**: 3-5

## 🎯 צעדים ראשונים

### ✅ רשימת בדיקות
- [ ] MT5 מותקן ופועל
- [ ] חשבון דמו פעיל
- [ ] הבוט מתחבר בהצלחה
- [ ] ממשק נגיש מהטלפון
- [ ] התראות פועלות (אופציונלי)

### 🔧 הגדרות מומלצות למתחילים
```bash
# הגדרות שמרניות לתחילת דרך
MAX_RISK_PER_TRADE=0.01          # 1% סיכון
MAX_DAILY_LOSS=0.03              # 3% הפסד יומי
MAX_OPEN_POSITIONS=3             # 3 פוזיציות מקסימום
AI_CONFIDENCE_THRESHOLD=0.8      # ביטחון גבוה
```

### 📊 זוגות מטבעות מומלצים
```bash
TRADING_SYMBOLS=EURUSD,GBPUSD    # התחל עם 2 זוגות בלבד
```

## 🚨 אזהרות חשובות

### ⚠️ לפני מסחר אמיתי
1. **תמיד התחל עם חשבון דמו**
2. **בדוק את הבוט לפחות שבוע**
3. **הבן את האסטרטגיה**
4. **קבע מגבלות ברורות**

### 🔒 אבטחה בסיסית
1. **שנה את הסיסמה הדיפולטיבית**
2. **אל תחשוף את השרת לאינטרנט**
3. **השתמש ברשת WiFi מאובטחת**
4. **עשה גיבויים של ההגדרות**

## 🆘 פתרון בעיות מהיר

### הבוט לא מתחבר ל-MT5
```bash
# בדוק שMT5 פועל
ps aux | grep terminal64  # Linux
tasklist | findstr terminal64  # Windows

# בדוק הגדרות בקובץ .env
cat .env | grep MT5
```

### לא מגיע לממשק מהטלפון
```bash
# מצא את IP של המחשב
ip addr show  # Linux
ipconfig      # Windows

# בדוק שהפורט פתוח
netstat -tulpn | grep 8080
```

### הבוט לא מבצע עסקאות
1. בדוק שהשוק פתוח
2. ודא שיש יתרה בחשבון
3. בדוק רמת ביטחון AI
4. ודא שהגדרות סיכון לא מגבילות

## 📞 קבלת עזרה

### תיעוד מלא
- `README_TRADING_BOT.md` - תיעוד כללי
- `INSTALLATION_GUIDE.md` - מדריך התקנה מפורט

### קהילה ותמיכה
- פתח Issue ב-GitHub
- הצטרף לקבוצת Telegram
- בדוק FAQ באתר

## 🎉 מזל טוב!

**הבוט שלך מוכן לפעולה! זכור:**
- התחל קטן וגדל בהדרגה
- למד מכל עסקה
- שמור על משמעת
- תמיד השתמש בניהול סיכונים

**מסחר מוצלח! 📈**