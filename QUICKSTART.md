# 🚀 מדריך התחלה מהירה

## התקנה מהירה (3 דקות)

### 1. התקנת תלותיות
```bash
pip install -r requirements.txt
```

### 2. הגדרת MT5
צור קובץ `.env`:
```bash
cp .env.example .env
```

ערוך את `.env` והכנס את פרטי MT5 שלך.

### 3. הפעלה
```bash
python run.py
```

### 4. גישה מהטלפון
פתח דפדפן בטלפון ונוחת לכתובת:
- **ברשת מקומית:** `http://<ip-address>:8000`
- לדוגמה: `http://192.168.1.100:8000`

למציאת כתובת IP:
- **Linux/Mac:** `hostname -I` או `ifconfig`
- **Windows:** `ipconfig`

## שימוש בסיסי

### שלב 1: התחברות
1. פתח את הטאב "הגדרות"
2. הכנס פרטי MT5
3. לחץ "התחבר ל-MT5"

### שלב 2: ביצוע עסקה
1. עבור לטאב "מסחר"
2. בחר סמל (למשל `EURUSD`)
3. בחר סוג עסקה (קנייה/מכירה)
4. הגדר כמות (0.01 = micro lot)
5. לחץ "ביצוע עסקה"

### שלב 3: ניתוח AI
1. עבור לטאב "AI ניתוח"
2. הכנס סמל
3. לחץ "קבל ניתוח"
4. קרא את ההמלצות

## טיפים

✅ **התחל עם חשבון Demo**
- אל תשתמש בחשבון אמיתי בהתחלה

✅ **אימון מודל AI**
- לפני שימוש ב-AI, אמן את המודל על נתונים היסטוריים
- השתמש בלפחות 1000 תקופות

✅ **בדיקת חיבור**
- ודא ש-MT5 מותקן ופועל לפני ההתחברות

✅ **גישה מהטלפון**
- ודא שהמחשב והטלפון באותה רשת WiFi
- אם יש Firewall, פתח את פורט 8000

## פתרון בעיות מהיר

**לא מתחבר ל-MT5?**
- ודא ש-MT5 פועל
- בדוק פרטי התחברות

**לא רואה ממשק בטלפון?**
- בדוק שהשרת פועל
- בדוק שכתובת IP נכונה
- ודא שהטלפון והמחשב באותה רשת

**שגיאת Python?**
- ודא ש-Python 3.8+
- הפעל: `pip install --upgrade pip`
- התקן מחדש: `pip install -r requirements.txt`

## שלבים מתקדמים

### אימון מודל AI מותאם אישית
1. עבור לטאב "AI ניתוח"
2. בחר סמל לניתוח
3. הכנס מספר תקופות (1000-5000)
4. לחץ "אימון מודל"
5. המתן לסיום (יכול לקחת כמה דקות)

### שימוש ב-API
```bash
# בדיקת חיבור
curl http://localhost:8000/api/v1/health

# קבלת מחיר
curl http://localhost:8000/api/v1/mt5/price/EURUSD

# ניתוח AI
curl http://localhost:8000/api/v1/ai/predict/EURUSD
```

## בטיחות

⚠️ **חשוב לזכור:**
- לעולם אל תחלוק את פרטי החשבון
- השתמש בחשבון Demo לבדיקות
- בדוק את הקוד לפני שימוש בחשבון אמיתי
- המסחר הוא בסיכון - היה אחראי

---

**רוצה עזרה נוספת?** קרא את [README.md](README.md) המלא.
