# Module: `src/core/setcore.py`

Import path: `src.core.setcore`

## Module docstring

No docstring provided.

## Basic import example

```python
from src.core.setcore import <symbol>
# <symbol> can be a function or class from this module
```

## Functions

### `definepath()`

No docstring provided.

```python
from src.core.setcore import definepath
result = definepath()
```

### `check_os()`

No docstring provided.

```python
from src.core.setcore import check_os
result = check_os()
```

### `setprompt(category, text)`

No docstring provided.

```python
from src.core.setcore import setprompt
result = setprompt(category=..., text=...)
```

### `yesno_prompt(category, text)`

No docstring provided.

```python
from src.core.setcore import yesno_prompt
result = yesno_prompt(category=..., text=...)
```

### `return_continue()`

No docstring provided.

```python
from src.core.setcore import return_continue
result = return_continue()
```

### `debug_msg(currentModule, message, msgType)`

No docstring provided.

```python
from src.core.setcore import debug_msg
result = debug_msg(currentModule=..., message=..., msgType=...)
```

### `mod_name()`

No docstring provided.

```python
from src.core.setcore import mod_name
result = mod_name()
```

### `print_status(message)`

No docstring provided.

```python
from src.core.setcore import print_status
result = print_status(message=...)
```

### `print_info(message)`

No docstring provided.

```python
from src.core.setcore import print_info
result = print_info(message=...)
```

### `print_info_spaces(message)`

No docstring provided.

```python
from src.core.setcore import print_info_spaces
result = print_info_spaces(message=...)
```

### `print_warning(message)`

No docstring provided.

```python
from src.core.setcore import print_warning
result = print_warning(message=...)
```

### `print_error(message)`

No docstring provided.

```python
from src.core.setcore import print_error
result = print_error(message=...)
```

### `get_version()`

No docstring provided.

```python
from src.core.setcore import get_version
result = get_version()
```

### `detect_public_ip()`

Helper function to auto-detect our public IP(v4) address.

```python
from src.core.setcore import detect_public_ip
result = detect_public_ip()
```

### `validate_ip(address)`

Validates that a given string is an IPv4 dotted quad.

```python
from src.core.setcore import validate_ip
result = validate_ip(address=...)
```

### `meta_path()`

No docstring provided.

```python
from src.core.setcore import meta_path
result = meta_path()
```

### `meta_database()`

No docstring provided.

```python
from src.core.setcore import meta_database
result = meta_database()
```

### `grab_ipaddress()`

No docstring provided.

```python
from src.core.setcore import grab_ipaddress
result = grab_ipaddress()
```

### `cleanup_routine()`

No docstring provided.

```python
from src.core.setcore import cleanup_routine
result = cleanup_routine()
```

### `update_set()`

No docstring provided.

```python
from src.core.setcore import update_set
result = update_set()
```

### `help_menu()`

No docstring provided.

```python
from src.core.setcore import help_menu
result = help_menu()
```

### `date_time()`

No docstring provided.

```python
from src.core.setcore import date_time
result = date_time()
```

### `generate_random_string(low, high)`

No docstring provided.

```python
from src.core.setcore import generate_random_string
result = generate_random_string(low=..., high=...)
```

### `site_cloner(website, exportpath, *args)`

No docstring provided.

```python
from src.core.setcore import site_cloner
result = site_cloner(website=..., exportpath=...)
```

### `start_web_server(directory)`

No docstring provided.

```python
from src.core.setcore import start_web_server
result = start_web_server(directory=...)
```

### `start_web_server_unthreaded(directory)`

No docstring provided.

```python
from src.core.setcore import start_web_server_unthreaded
result = start_web_server_unthreaded(directory=...)
```

### `java_applet_attack(website, port, directory)`

No docstring provided.

```python
from src.core.setcore import java_applet_attack
result = java_applet_attack(website=..., port=..., directory=...)
```

### `teensy_pde_generator(attack_method)`

No docstring provided.

```python
from src.core.setcore import teensy_pde_generator
result = teensy_pde_generator(attack_method=...)
```

### `windows_root()`

No docstring provided.

```python
from src.core.setcore import windows_root
result = windows_root()
```

### `log(error)`

No docstring provided.

```python
from src.core.setcore import log
result = log(error=...)
```

### `upx(path_to_file)`

No docstring provided.

```python
from src.core.setcore import upx
result = upx(path_to_file=...)
```

### `show_banner(define_version, graphic)`

No docstring provided.

```python
from src.core.setcore import show_banner
result = show_banner(define_version=..., graphic=...)
```

### `show_graphic()`

No docstring provided.

```python
from src.core.setcore import show_graphic
result = show_graphic()
```

### `set_check()`

No docstring provided.

```python
from src.core.setcore import set_check
result = set_check()
```

### `menu_back()`

No docstring provided.

```python
from src.core.setcore import menu_back
result = menu_back()
```

### `custom_template()`

No docstring provided.

```python
from src.core.setcore import custom_template
result = custom_template()
```

### `check_length(choice, max)`

No docstring provided.

```python
from src.core.setcore import check_length
result = check_length(choice=..., max=...)
```

### `is_valid_ip(ip)`

No docstring provided.

```python
from src.core.setcore import is_valid_ip
result = is_valid_ip(ip=...)
```

### `is_valid_ipv4(ip)`

No docstring provided.

```python
from src.core.setcore import is_valid_ipv4
result = is_valid_ipv4(ip=...)
```

### `is_valid_ipv6(ip)`

Validates IPv6 addresses.

```python
from src.core.setcore import is_valid_ipv6
result = is_valid_ipv6(ip=...)
```

### `kill_proc(port, flag)`

No docstring provided.

```python
from src.core.setcore import kill_proc
result = kill_proc(port=..., flag=...)
```

### `check_config(param)`

No docstring provided.

```python
from src.core.setcore import check_config
result = check_config(param=...)
```

### `copyfolder(sourcePath, destPath)`

No docstring provided.

```python
from src.core.setcore import copyfolder
result = copyfolder(sourcePath=..., destPath=...)
```

### `check_options(option)`

No docstring provided.

```python
from src.core.setcore import check_options
result = check_options(option=...)
```

### `update_options(option)`

No docstring provided.

```python
from src.core.setcore import update_options
result = update_options(option=...)
```

### `socket_listener(port)`

No docstring provided.

```python
from src.core.setcore import socket_listener
result = socket_listener(port=...)
```

### `generate_powershell_alphanumeric_payload(payload, ipaddr, port, payload2)`

No docstring provided.

```python
from src.core.setcore import generate_powershell_alphanumeric_payload
result = generate_powershell_alphanumeric_payload(payload=..., ipaddr=..., port=..., payload2=...)
```

### `generate_shellcode(payload, ipaddr, port)`

No docstring provided.

```python
from src.core.setcore import generate_shellcode
result = generate_shellcode(payload=..., ipaddr=..., port=...)
```

### `shellcode_replace(ipaddr, port, shellcode)`

No docstring provided.

```python
from src.core.setcore import shellcode_replace
result = shellcode_replace(ipaddr=..., port=..., shellcode=...)
```

### `exit_set()`

No docstring provided.

```python
from src.core.setcore import exit_set
result = exit_set()
```

### `metasploit_shellcode(payload, ipaddr, port)`

No docstring provided.

```python
from src.core.setcore import metasploit_shellcode
result = metasploit_shellcode(payload=..., ipaddr=..., port=...)
```

### `encryptAES(secret, data)`

No docstring provided.

```python
from src.core.setcore import encryptAES
result = encryptAES(secret=..., data=...)
```

### `check_ports(filename, port)`

No docstring provided.

```python
from src.core.setcore import check_ports
result = check_ports(filename=..., port=...)
```

### `setdir()`

No docstring provided.

```python
from src.core.setcore import setdir
result = setdir()
```

### `ip2bin(ip)`

No docstring provided.

```python
from src.core.setcore import ip2bin
result = ip2bin(ip=...)
```

### `dec2bin(n, d=None)`

No docstring provided.

```python
from src.core.setcore import dec2bin
result = dec2bin(n=...)
```

### `bin2ip(b)`

No docstring provided.

```python
from src.core.setcore import bin2ip
result = bin2ip(b=...)
```

### `printCIDR(c)`

No docstring provided.

```python
from src.core.setcore import printCIDR
result = printCIDR(c=...)
```

### `validateCIDRBlock(b)`

No docstring provided.

```python
from src.core.setcore import validateCIDRBlock
result = validateCIDRBlock(b=...)
```

### `get_sql_port(host)`

No docstring provided.

```python
from src.core.setcore import get_sql_port
result = get_sql_port(host=...)
```

### `capture(func, *args, **kwargs)`

Capture the output of func when called with the given arguments.
The function output includes any exception raised. capture returns
a tuple of (function result, standard output, standard error).

```python
from src.core.setcore import capture
result = capture(func=...)
```

### `check_backbox()`

No docstring provided.

```python
from src.core.setcore import check_backbox
result = check_backbox()
```

### `check_kali()`

No docstring provided.

```python
from src.core.setcore import check_kali
result = check_kali()
```

### `applet_choice()`

No docstring provided.

```python
from src.core.setcore import applet_choice
result = applet_choice()
```

### `module_reload(module)`

No docstring provided.

```python
from src.core.setcore import module_reload
result = module_reload(module=...)
```

### `input(string)`

No docstring provided.

```python
from src.core.setcore import input
result = input(string=...)
```

### `fetch_template()`

No docstring provided.

```python
from src.core.setcore import fetch_template
result = fetch_template()
```

### `tail(filename)`

No docstring provided.

```python
from src.core.setcore import tail
result = tail(filename=...)
```

### `powershell_encodedcommand(ps_attack)`

No docstring provided.

```python
from src.core.setcore import powershell_encodedcommand
result = powershell_encodedcommand(ps_attack=...)
```

## Classes

### `create_menu`

No docstring provided.

```python
from src.core.setcore import create_menu
obj = create_menu(self=..., text=..., menu=...)
# Call a method (if available):
# (no public methods detected)
```
