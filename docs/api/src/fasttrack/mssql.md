# Module: `src/fasttrack/mssql.py`

Import path: `src.fasttrack.mssql`

## Module docstring

No docstring provided.

## Basic import example

```python
from src.fasttrack.mssql import <symbol>
# <symbol> can be a function or class from this module
```

## Functions

### `brute(ipaddr, username, port, wordlist)`

No docstring provided.

```python
from src.fasttrack.mssql import brute
result = brute(ipaddr=..., username=..., port=..., wordlist=...)
```

### `deploy_hex2binary(ipaddr, port, username, password)`

No docstring provided.

```python
from src.fasttrack.mssql import deploy_hex2binary
result = deploy_hex2binary(ipaddr=..., port=..., username=..., password=...)
```

### `cmdshell(ipaddr, port, username, password, option)`

No docstring provided.

```python
from src.fasttrack.mssql import cmdshell
result = cmdshell(ipaddr=..., port=..., username=..., password=..., option=...)
```
