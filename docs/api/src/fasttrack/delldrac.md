# Module: `src/fasttrack/delldrac.py`

Import path: `src.fasttrack.delldrac`

## Module docstring

No docstring provided.

## Basic import example

```python
from src.fasttrack.delldrac import <symbol>
# <symbol> can be a function or class from this module
```

## Functions

### `login_drac(ipaddr_single)`

No docstring provided.

```python
from src.fasttrack.delldrac import login_drac
result = login_drac(ipaddr_single=...)
```

### `login_chassis(ipaddr_single)`

No docstring provided.

```python
from src.fasttrack.delldrac import login_chassis
result = login_chassis(ipaddr_single=...)
```

### `is_valid_ip(ip)`

No docstring provided.

```python
from src.fasttrack.delldrac import is_valid_ip
result = is_valid_ip(ip=...)
```

### `ip2bin(ip)`

No docstring provided.

```python
from src.fasttrack.delldrac import ip2bin
result = ip2bin(ip=...)
```

### `dec2bin(n, d=None)`

No docstring provided.

```python
from src.fasttrack.delldrac import dec2bin
result = dec2bin(n=...)
```

### `bin2ip(b)`

No docstring provided.

```python
from src.fasttrack.delldrac import bin2ip
result = bin2ip(b=...)
```

### `scan(ipaddr)`

No docstring provided.

```python
from src.fasttrack.delldrac import scan
result = scan(ipaddr=...)
```

## Classes

### `bcolors`

No docstring provided.

```python
from src.fasttrack.delldrac import bcolors
obj = bcolors()
# Call a method (if available):
obj.disable()
```

#### Methods

- `disable(self)`
  - No docstring provided.
